const { servidores, Elo, Ranked, Tier } = require("./Constants")
const { VeigaristaJS } = require("./Controller/Veigarista")

module.exports = {
    VeigaristaJS,
    servidores,
    Elo,
    Ranked,
    Tier
}