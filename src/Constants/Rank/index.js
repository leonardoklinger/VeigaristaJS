const { Elo, EloTraducao } = require("./Elo")
const { Ranked, RankedTraducao } = require("./Ranked")
const { Tier } = require("./Tier")

module.exports = {
    Elo, 
    Ranked, 
    Tier, 
    EloTraducao,
    RankedTraducao
}